# ComfyUI-UltraShape1 Dependencies
# Note: torch, numpy, PIL, transformers, diffusers, tqdm, safetensors
# are already provided by ComfyUI

# Core dependencies
trimesh>=4.0.0
omegaconf>=2.3.0
einops>=0.8.0
pymeshlab

# Optional: Background removal (can use ComfyUI's built-in if available)
rembg

diffusers
pytorch-lightning
# Mesh processing (may be required for some features)
# diso>=0.1.4
